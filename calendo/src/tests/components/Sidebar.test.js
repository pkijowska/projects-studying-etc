import React from 'react';
import { shallow } from 'enzyme';
import SideBar from '../../components/SideBar';

test('renders correctly', () => {
  // const tree = renderer
  //   .create(<SideBar todayNr={18} year={2022}  month={"July"} />)
  //   .toJSON();
  // expect(tree).toMatchInlineSnapshot();
});
